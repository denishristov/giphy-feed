export const PAGINATION_OFFSET: number = 30;

export const GIF_MARGIN: number = 12;

export const GIF_MAX_WIDTH: number = 400;

export const HEADER_HEIGHT: number = 66;

export const MAX_ITEMS_PER_ROW: number = 3;

export const INITIAL_SEARCH_TERM: string = "dog";

/**
 * If window's width is bigger than the threshold we can
 * fit a grid with at least 2 columns on the screen.
 */
export const LIST_VIEW_THRESHOLD = 2 * GIF_MAX_WIDTH + 3 * GIF_MARGIN;
